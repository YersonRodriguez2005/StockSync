# 1. Dependencias (Pesadas y autogenerables)
node_modules/
.pnp
.pnp.js

# 2. Variables de entorno y Secretos (Crítico para la seguridad)
.env
.env.local
.env.development
.env.production

# 3. Compilados y Builds (Generados localmente o en el servidor)
dist/
build/
out/
*.apk
*.aab

# 4. Logs y Debugging (Registros de errores locales)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# 5. Archivos del Sistema Operativo
.DS_Store
Thumbs.db